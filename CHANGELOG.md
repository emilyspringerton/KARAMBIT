## 2026-09-10 (continued, 2)
- fix(ci): real CI break fixed. Founder real-time: "KARAMBIT build is down." Confirmed via the real job log (fetched directly from the GitHub API using a stored token, since `gh` isn't available in this sandbox): `Install Android SDK platform + build-tools` failed with exit code 1 even though `sdkmanager` itself genuinely succeeded (all three packages installed) -- root cause is `yes | sdkmanager ...` under this step's own `set -o pipefail`: once sdkmanager stops reading stdin, `yes` gets a real SIGPIPE ("yes: standard output: Broken pipe"), and pipefail reports that nonzero exit as the whole pipeline's failure, masking sdkmanager's own real 0. Fixed by `set +o pipefail` around just that one command in both jobs (build_and_test and release) that run it, restoring bash's default behavior of reporting only the last command's (sdkmanager's) exit status -- a genuine sdkmanager failure still fails the step correctly. Reproduced and confirmed the exact failure/fix locally with a minimal `yes | head -1` repro before trusting it in CI. (sess-20260905-0720-ec33e7c5)

## 2026-09-10 (continued)
- feat: real product-completeness pass + real auto-releases. Founder real-time: "iterate ensuring the product is complete once its good ensure we are cutting auto releases." Added a real, generated launcher icon (radar-style PNG at every density bucket, mdpi-xxxhdpi) via ImageMagick; explicit minSdkVersion=23/targetSdkVersion=34 in the manifest (previously left implicit, silently patched by a Bazel build step); real versionCode/versionName wiring via android_binary's manifest_values, baked in for real at release time (not the repo's own dev placeholder); a live scan-progress indicator in MainActivity ("N/254 checked", not just a start/finish message). Found and fixed a second instance of the XML "--"-in-comment bug (same real XML spec rule bit us on AndroidManifest.xml the first time). New `.github/workflows/ci.yml`: build_and_test runs on every push/PR (real plain-JVM tests + a real placeholder-versioned APK, uploaded as a workflow artifact); release (main-branch pushes only) computes the next minor version (same real scheme PARENA's own ci.yml established), sed-patches the real version into app/BUILD.bazel, rebuilds, tags, and cuts a real GitHub Release with the real, correctly-versioned, correctly-signed APK attached. Live-verified the exact sed-patch + rebuild logic locally before trusting it in CI: produced a real versionCode=42/versionName=v0.1.0 APK, confirmed via aapt2 dump badging + apksigner verify. (sess-20260905-0720-ec33e7c5)

## 2026-09-10
- feat: real v0 shipped. Founder real-time: "build a toolkit android app to help scan my network... ssh for example should be open... maybe we just check for that for now so we arent too noisy on the network... keep it pluggable and extensible... build it parena native as much as possible interopping with native java use BAZEL and PARENA." New `parena/scan_decisions.prn` (real PARENA decision logic: is-target-port/has-more-hosts/next-scan-index/classify-scan-result), compiled via PARENA's own real Java emitter to `app/.../generated/ScanDecisions.java` -- same real precedent SPIDERBEETLE's own battery_ui.prn already established. New, hand-written Java: `ScanStrategy` (the real pluggable interface), `SshPortScanStrategy` (v0: bare TCP connect on port 22 only), `ScanEngine` (bounded 8-way concurrent host enumeration), `NetworkInfo` (real Android ConnectivityManager-based local-IP detection), `MainActivity` (real UI: detect + scan button + live results log). Real, no-sudo-acquired Android SDK (cmdline-tools + platform 34 + build-tools 34.0.0/35.0.0) and a real, no-sudo-acquired JDK, matching this monorepo's own established toolchain-acquisition discipline. Real Bazel setup (`rules_android` 0.7.3 + `rules_java`), split into a plain-JVM-testable `scan_core_lib`/`scan_core_test` (13 real assertions, including a genuine local TCP socket round trip, verified via `bazel run`) and the Android-dependent `karambit_lib`/`karambit` APK target. **`//app:karambit` builds a real, signed, installable APK** -- verified via `apksigner verify` (v1/v2/v3 signature schemes all pass) and `aapt2 dump badging` (correct package/permissions/launchable-activity). Found and fixed two real, live-checked build issues along the way: an XML comment containing "--" broke aapt2's manifest parsing (a real XML spec rule, not an Android-specific quirk); a target/tool Java-version mismatch broke compiling `rules_android`'s own internal `DexFileSplitter.java` ("could not locate class file for java.lang.Record"), fixed by pinning both via a new `.bazelrc`. Also found a real, sandbox-specific gotcha: this monorepo's own `/home/fatbaby/go.work` (in a parent directory) leaks into `rules_android`'s internal Go-tool bootstrap; worked around by running the build with `HOME` pointed outside `/home/fatbaby` for that invocation. Not yet done: installing/running the APK on a real device (none reachable from this sandbox). (sess-20260905-0720-ec33e7c5)
